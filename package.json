{
  "name": "supabase-kit",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "rm -rf build && svelte-kit build && touch dist/.nojekyll",
    "preview": "svelte-kit preview",
    "deploy": "npm run build && npx gh-pages -d dist -t true"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^1.0.0-next.13",
    "@sveltejs/kit": "next",
    "gh-pages": "^3.2.3",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^1.21.0"
  }
}